{
  "name": "xenova-transformers-server",
  "version": "1.0.0",
  "description": "Server to generate embeddings using Xenova Transformers",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env --watch src/server.js"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
